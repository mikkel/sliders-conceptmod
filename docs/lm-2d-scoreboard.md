# 2-D / high-D / sheet scoreboard

Generated by `analysis/slider2d/run_lm_scoreboard.py`. CPU only, no Hub,
no GPU, no Music 3 weights. Does not change the live trainer default.

This is the #23 compiled board, updated in place against the
2026-08-25 Music 3 listens. The leftover unused-gender sheet is
still a column. It is no longer the energy stand-in.

## The compiled gate

A method WORKS only if it stays on the divergent two-track sheet (energy-v4 stand-in: leftover ê *is* the poles) AND on the close-pair rollout (gender-v4 stand-in: one-token KL is not enough). Unused-gender leftover leak is logged; it is not the energy stand-in. Genre/BPM that are the poles are not a leak fail. High pair-odd cos and ±1 collapse are logged, never scored. works-on-energy-only means the divergent cell passes and the close pair does not (energy-v18). works-on-gender-only means the close pair sings and the two-track cell does not.

- divergent on-sheet kept: `≥ 0.9`
- off-sheet / blend mass: `≤ 0.05`
- close-pair rollout kept: `≥ 0.9`
- unused-gender leftover scored as energy: `False`
- pair-odd cos scored: `False`
- ±1 collapse scored: `False`

## Live exam (2026-08-25, mikkel main f8d71a8 / #24)

Three listens. The board has to rank the matching fixture rows
the same way the ear did. Starred rows in the plot are these.

| live run | recipe | cell | predicted | listen | match |
|---|---|---|---|---|---|
| **energy-lm-v16** | `semantic_kl_sub_e` (`semantic_kl` + `faithful_sub_e`) | divergent | **FAIL** | FAIL | yes |
| **energy-lm-v18** | `semantic_kl_poles` (`semantic_kl` + `faithful`) | divergent | **PASS** | PASS | yes |
| **gender-lm-v16** | `semantic_kl_poles` (`semantic_kl` + `faithful`) | close | **FAIL/flagged** | FAIL | yes |

Why those three land that way:

- **energy-v16 / `faithful_sub_e` on two tracks.** Leftover ê is
  `Pop-punk mix, BPM 168` / `Ambient lullaby mix, BPM 52` — most
  of `pos−neg`. `mid ± â` with `mid = ½(h++h−)` is a third song.
  The student aims at the blend and sings off-caption.
- **energy-v18 / `faithful` on the same pair.** The poles *are*
  the genre/BPM ride. One-token KL has a huge first-token gap to
  match, so the student actually moves. Unused-gender leak inside
  a same-song pair is a different cell; it does not fail this one.
- **gender-v16 / the same KL recipe on a close pair.** First-token
  policies already nearly match (same song). KL looks done at
  p% 0.5–0.7 / loss ~0.01 while the hidden residual stays large.
  Teacher-forced step-1 still needs that residual and garbles.
  A cheap metric already sees it: `kl_small_hidden_far`
  (`kl_pole` ≤ 0.15 and live `pperc` ≥ 0.40).

## Short verdict

**Works, in order:** `faithful_attrs`, `faithful_raw`, `hidden_beta1`. Hidden MSE onto the *raw* poles passes both exam cells. That is the untrained gender card (`--lm_target faithful --pole_mode hidden`).
**Works on energy only:** `semantic_kl_poles`. Divergent two-track sheet holds (energy-v18 listen). Close-pair rollout does not (gender-v16 listen).

**Fails:** 16 recipes, including `faithful_sub_e` / `semantic_kl_sub_e` on two tracks (energy-v16), the live midpoint (`pair_odd_midpoint` / `--lm_target v9`), `#20` `pair_odd_sub_e`, hub, and hold-ê. A perfect pair-odd lock is still the failure mode. ê-cleaning a divergent pair is now a failure mode too.

## Table

Sorted by compiled verdict, then unused-gender leak, then divergent
on-sheet. Pair-odd cos, unused-gender leak, and ±1 are **logged,
never scored** as the energy stand-in. Exam rows are named in the
live-exam column.

| recipe | live exam | unused-gender leak *(log)* | divergent kept | off-sheet | close rollout | pperc | KL-small / hidden-far | pair-odd cos *(log)* | compiled |
|---|---|---:|---:|---:|---:|---:|---|---:|---|
| `faithful_attrs` | — | +0.000 | 1.007 | 0.000 | 0.997 | 0.10 | no | +0.696 | **works** |
| `faithful_raw` | — | +0.228 | 1.007 | 0.000 | 0.997 | 0.10 | no | +0.696 | **works** |
| `hidden_beta1` | — | +0.228 | 1.007 | 0.000 | 0.997 | 0.10 | no | +0.696 | **works** |
| `semantic_kl_poles` | energy-lm-v18, gender-lm-v16 | +0.226 | 1.000 | 0.000 | 0.795 | 0.74 | yes | +0.616 | works-on-energy-only |
| `faithful_sub_e` | — | +0.000 | 0.827 | 0.000 | N/A | 0.10 | no | +0.063 | **fails** |
| `semantic_kl_sub_e` | energy-lm-v16 | +0.000 | 0.817 | 0.000 | N/A | 0.29 | no | +0.050 | **fails** |
| `project_rich_u` | — | +0.000 | 0.620 | 0.043 | 0.897 | 0.10 | no | +1.000 | **fails** |
| `project_short_u` | — | +0.000 | 0.620 | 0.043 | 0.897 | 0.10 | no | +1.000 | **fails** |
| `pair_odd_sub_e` | — | +0.000 | 0.074 | 0.097 | 0.897 | 0.10 | no | +0.258 | **fails** |
| `hold_e_perp_l8` | — | +0.006 | 0.620 | 0.043 | 0.897 | 0.10 | no | +1.000 | **fails** |
| `hold_e_raw_l8` | — | +0.154 | 0.620 | 0.043 | 0.897 | 0.10 | no | +1.000 | **fails** |
| `semantic_kl_midpoint` | — | +0.227 | 0.616 | 0.043 | 0.433 | 0.99 | no | +0.977 | **fails** |
| `pair_odd_midpoint` | — | +0.228 | 0.620 | 0.043 | 0.897 | 0.10 | no | +1.000 | **fails** |
| `hold_e_raw_synonym_l8` | — | +0.320 | 0.620 | 0.043 | 0.897 | 0.10 | no | +1.000 | **fails** |
| `leftover_hold_l8` | — | +0.504 | 0.620 | 0.043 | N/A | 0.10 | no | +1.000 | **fails** |
| `leftover_hold_l1` | — | +0.529 | 0.620 | 0.043 | N/A | 0.10 | no | +1.000 | **fails** |
| `hold_e_perp_l1` | — | +0.694 | 0.620 | 0.043 | 0.897 | 0.10 | no | +1.000 | **fails** |
| `hold_e_raw_l1` | — | +0.694 | 0.620 | 0.043 | 0.897 | 0.10 | no | +1.000 | **fails** |
| `hub` | — | +1.388 | 0.620 | 0.043 | 0.897 | 0.10 | no | +1.000 | **fails** |
| `gender_like_no_e` | — | N/A | 0.620 | 0.043 | 0.897 | 0.10 | no | +1.000 | **fails** |

![divergent sheet vs close rollout](lm-2d-scoreboard/scoreboard.png)

## What each row is

- `faithful_attrs` — faithful + attributes / pin unused. data fix: unused gender/BPM pinned in the captions. Poles become the gender-like sheet. Fixture: Field2D attrs + rich pin-both + gender sheet. (polarity -0.080; c+ distinct +0.735)
- `faithful_raw` — faithful / v6 raw poles. raw-pole MSE. Caption target; unused ê rides along. Fixture: sheet leftover + sheet gender.
- `hidden_beta1` — pair-odd β=1 / symmetric --common_beta 1. lm_hidden_targets(symmetric, β=1) is the raw poles. Sheet-good, still leaks ê. Fixture: sheet leftover faithful ≡ β=1 + gender hidden_beta1.
- `semantic_kl_poles` — semantic_kl onto real poles. Live energy-v18 PASS / gender-v16 FAIL. Raw poles. Genre/BPM that are the poles is not a leak fail; close-pair one-token KL is kl_small_hidden_far. Fixture: sheet leftover + sheet gender. (polarity +0.125)
- `faithful_sub_e` — faithful_sub_e (ê-cleaned real poles, hidden MSE). On unused-gender leftover this is a near-caption. On two tracks leftover ê *is* the poles, so mid ± â is a third song (energy-v16 listen fail). Fixture: sheet leftover. (polarity +0.105; c+ distinct +0.621)
- `semantic_kl_sub_e` — semantic_kl onto ê-cleaned poles. Live energy-v16. Same blend teacher as faithful_sub_e on two tracks. KL matching a third song still sings off-caption. Fixture: sheet leftover. (polarity +0.209; c+ distinct +0.522)
- `project_rich_u` — project rich û (oracle intended span). oracle û = span{short, slider adjectives}. Still a midpoint in the intended plane, so c is dropped; sheet inherited from pair-odd. Fixture: rich project_rich.
- `project_short_u` — project short û. leak-0 by dropping everything ⊥ short û, including the singer and slider adjectives. Still a midpoint (c deleted); sheet inherited from pair-odd. Fixture: rich project_short + live gender always_project_hold.
- `pair_odd_sub_e` — pair_odd_sub_e (#20, midpoint − ê_⊥). λ→∞ hold in one step. Leak 0; further off-caption than pair-odd. Fixture: sheet leftover pair_odd_sub_e + high-D leftover subtract. (content-cos +0.893; c+ distinct +0.809; loss 0.000)
- `hold_e_perp_l8` — hold-ê ê_⊥û λ=8 (live v9). current leftover-ê default. Fixes unused-axis leak, not the sheet. Fixture: sheet leftover hold-ê + overlap ê_⊥û λ=8. (c+ distinct +0.696; loss 0.852)
- `hold_e_raw_l8` — hold-ê raw λ=8 (leftover ê). on leftover ê, raw ≡ ê_⊥û. Leak can look small; sheet is still the midpoint. Fixture: overlap leftover raw λ=8 + hold-ê sheet. (c+ distinct +0.696; loss 0.852)
- `semantic_kl_midpoint` — semantic_kl onto midpoint. KL is not the fix. The target point is. Fixture: sheet leftover + sheet gender. (c+ distinct +0.901)
- `pair_odd_midpoint` — pair-odd / v9 hidden midpoint. live --lm_target v9 teacher t± = h0 ± a. Not a caption. Fixture: sheet leftover + sheet gender.
- `hold_e_raw_synonym_l8` — hold-ê raw λ=8 (synonym ê). ê restates the poles. Raw hold punches the slider. Fixture: overlap synonym raw λ=8 + midpoint sheet. (c+ distinct +0.798; loss 1.270)
- `leftover_hold_l8` — energy-like leftover hold-ê λ=8 (high-D). λ=8 barely moves leftover leak vs λ=1. Sheet still the midpoint. Fixture: high-D leftover λ=8 + sheet hold-ê. (content-cos +0.893; c+ distinct +0.822; loss 0.120)
- `leftover_hold_l1` — energy-like leftover hold-ê λ=1 (high-D). one leftover caption pair cannot name the whole unused remainder. Fixture: high-D leftover λ=1 + sheet hold λ=1. (content-cos +0.884; c+ distinct +0.885; loss 0.099)
- `hold_e_perp_l1` — hold-ê ê_⊥û λ=1. live-weak λ. Sheet still aims at h0 ± a. Fixture: overlap leftover ê_⊥û λ=1 + sheet hold λ=1. (c+ distinct +0.936; loss 0.489)
- `hold_e_raw_l1` — hold-ê raw λ=1. λ=1 is too soft on leftover ê. Teacher is still the midpoint. Fixture: overlap leftover raw + midpoint sheet. (c+ distinct +0.936; loss 0.489)
- `hub` — hub (published floor + anchor). same odd teacher as pair-odd; even blend-back does not take ê out of a. Sheet inherited from the midpoint. Fixture: live energy/gender + pair-odd sheet.
- `gender_like_no_e` — gender-like (no ê, hold 0). clean pair, hold 0. Live gender-lm-v4 log. Midpoint still deletes c. Fixture: sheet gender v9_hidden + high-D gender_like_no_e. (loss 0.009)

## Why #23 ranked the exam backwards

#23 scored leftover unused gender inside a *same-song* pair. On
that cell `faithful_sub_e` / `semantic_kl_sub_e` look perfect
(ê is unused, removing it keeps `c`) and `semantic_kl_poles`
fails for `leak_tok` (gender riding along). Live energy-v4 is the
other geometry: ê *is* the two tracks, so ê-cleaning *is* the
blend teacher, and genre/BPM movement is the slider.

The leftover unused-gender cell is still computed. It is not the
energy stand-in and it cannot promote a row.

## Why pair-odd cos is not in the score

A perfect `cos(d+, a)` / `cos(d+, d−)` is the midpoint recipe that
garbled live (v15 / v9). Pair-odd lock is logged, never scored.

## Next live card

If the board's overall winners have not been trained on gender-v4,
train **gender `--lm_target faithful --pole_mode hidden`** (hold 0,
no leak_*). That is `faithful_raw` / `hidden_beta1` here: hidden
MSE pins the continuation dim that one-token KL leaves behind.
Do not change the trainer default (`v9` / `hidden`). Do not rewrite
energy-v4 yamls. energy-v18 already showed `semantic_kl` +
`faithful` listens on the divergent pair.

## Related cells

- [lm-sheet-goodhart.md](lm-sheet-goodhart.md) — one-token sheet
  and the leftover unused-gender cell.
- [lm-live-cells.md](lm-live-cells.md) — gender-like vs energy-like
  hidden leak.
- [lm-highd-leftover.md](lm-highd-leftover.md) — leftover ê, λ·D/2,
  trainer c+.
- [lm-faithful-2d.md](lm-faithful-2d.md) — raw poles vs attributes.

## How to run

```bash
PYTHONPATH=. python analysis/slider2d/run_lm_scoreboard.py --out docs/lm-2d-scoreboard
PYTHONPATH=. pytest tests/test_lm_2d_scoreboard.py tests/test_lm_sheet_exam.py tests/test_lm_sheet_goodhart.py -q
```

CPU only. No Hub, no GPU, no Music 3 weights.

Sheet seed `0`, `400` Adam steps; other fixtures `200` steps.

