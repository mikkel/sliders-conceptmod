# Richer poles without hosing leak

Placeholder — regenerated by `analysis/slider2d/run_lm_rich.py` with
measured numbers from the CPU fixture. See that script and
`tests/test_lm_rich_2d.py`.

CPU only. No Hub, no GPU, no Music 3 weights.
